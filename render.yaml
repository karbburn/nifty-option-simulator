services:
  - type: web
    name: nifty-option-sim
    runtime: python
    buildCommand: pip install -e ".[web]"
    startCommand: uvicorn nifty_gap.web.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"